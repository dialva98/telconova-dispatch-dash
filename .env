# Backend API URL
VITE_API_URL=http://localhost:8080/api

# Usar datos mock para desarrollo (false para usar backend real)
# Backend real solo soporta: auth (login/register) y technicians (create/list)
# Mock API incluye: work orders, assignments, notifications
VITE_USE_MOCK_API=true
