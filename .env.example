# Backend API URL
# Por defecto apunta a localhost en desarrollo
# Cambia esto a la URL de tu backend en producción
VITE_API_URL=http://localhost:8080/api

# Usar datos mock (true) o backend real (false)
# Backend real soporta: POST /api/auth/login, POST /api/auth/register,
#                       POST /api/technicians/create, GET /api/technicians/all
# Mock API incluye funcionalidad completa: work orders, assignments, notifications
VITE_USE_MOCK_API=true
